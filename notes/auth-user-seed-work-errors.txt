
IMPLEMENTED:

- task 1: Models, utilities, and server setup
  * src/models/User.ts & Role.ts with Mongoose schemas
  * User fields: name, email, password, phone, role, isVerified, verificationToken, resetToken
  * Role fields: name, description, permissions array, isSystem flag
  * src/utils/auth.ts: password hashing, JWT generation (access + refresh tokens)
  * src/utils/email.ts: email notification stubs (sendVerificationEmail, sendResetEmail)
  * src/middleware/errorHandler.ts: global error handling with standard envelope
  * src/config/database.ts: MongoDB connection
  * Standard API response envelope in constants.ts
  * tsconfig.json with @ path alias for src/

- task 2: Controllers, routes, middleware, admin, and seeding
  * authController: register, login, verifyEmail, forgotPassword, resetPassword, refreshToken, getProfile, updateProfile
  * authRoutes: POST /register, /login, /verify-email, /forgot-password, /reset-password, /refresh-token, GET/PATCH /me
  * userController: getAllUsers, getUserById, deleteUser (admin functions)
  * userRoutes: GET /users (list), GET /users/:id (detail), DELETE /users/:id
  * auth middleware: protect (Bearer token verification), requirePermission (role-based authorization)
  * Swagger documentation for all endpoints with security schemes
  * seedRoles.ts: default roles (super_admin, sales_staff, inventory_staff, support_staff, marketing_staff, customer)
  * All responses follow standard envelope with success, data, message, error codes

ERRORS FIXED:
1. Missing TypeScript type definitions
   - Issue: @types/swagger-ui-express and @types/swagger-jsdoc not installed
   - Fix: npm install -D @types/swagger-ui-express @types/swagger-jsdoc
   
2. Invalid Express route pattern in app.use()
   - Issue: app.use('*', handler) throws PathError (Express doesn't support *)
   - Fix: Changed to app.use(handler) for catch-all 404 middleware

AUTHENTICATION FEATURES:
- JWT-based auth with 15min access token, 7day refresh token
- Password hashing with bcrypt (salt 10)
- Email verification flow with verification tokens
- Forgot/reset password flow with reset tokens
- Role-based access control with permission checking
- Protected endpoints with Bearer token validation
- User profile endpoints (GET /me, PATCH /me)
- Admin user management (list, view, delete)

SEED DATA:
- 6 default roles with permission mappings
- super_admin: wildcard (*) permission
- Each role tied to specific module permissions (products, orders, inventory, etc)"